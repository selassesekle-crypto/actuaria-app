# ActuarIA -- dependances COEUR (requises dans TOUT environnement).
# Le pipeline Non-Vie complet A1->A6 tourne avec ces seules dependances :
# GLM via statsmodels + ML de base (gbm / elasticnet / random_forest) via
# scikit-learn ; ingestion / preprocessing via pandas / numpy / scipy / pyarrow.
#
# Les modeles ML BOOSTES (xgboost / lightgbm / catboost), le DEEP LEARNING
# (torch), l'explicabilite SHAP et l'optimisation OPTUNA sont OPTIONNELS : le
# code les importe en try/except (flags XGBOOST_OK / LIGHTGBM_OK / CATBOOST_OK /
# SHAP_OK / OPTUNA_OK dans a4_ml, TORCH_OK dans a5_deep_learning) et DEGRADE
# GRACIEUSEMENT sans eux.
# --> declares dans requirements-optional.txt
#
# Installation complete :
#   pip install -r requirements.txt -r requirements-optional.txt

anthropic>=0.40.0
streamlit>=1.59.1
# ATTENTION VERSION : l'epingle etait == 5.18.0 alors que la gate tourne, et
# est verte, sur plotly 6.5.2. requirements.txt ne decrivait donc PAS
# l'environnement dans lequel les 876 tests passent. Le code n'emploie que
# `go.Figure`, `to_html` et `to_image`, stables de la 5 a la 6 ; 5.18.0 reste
# le plancher historique, mais SEULE la 6.5.2 est verifiee par la suite de
# tests actuelle. Le >= aligne aussi plotly sur la convention de ce fichier,
# ou toutes les autres dependances sont bornees par le bas.
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
pyarrow>=14.0.0
reportlab>=4.0.0
matplotlib>=3.7.0
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
python-docx>=1.1.0
lxml>=4.9.0
# PyYAML : PlanTarifaire.depuis_yaml (core/plan_tarifaire.py) charge les plans
# par `import yaml`. Dependance COEUR reelle, omise de ce fichier jusqu'ici --
# son absence a rendu la Gate 2 (core) ROUGE des le 24/08, quand les tests
# d'elasticite de core/ ont commence a appeler depuis_yaml en environnement CI
# (ou seul requirements.txt est installe, et ou PyYAML manquait donc).
pyyaml>=6.0
